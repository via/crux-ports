name=libftdi
version=1.0
release=1
source=(http://www.intra2net.com/en/developer/$name/download/${name}1-$version.tar.bz2)

build() {
  cd ${name}1-$version
  cmake -DCMAKE_INSTALL_PREFIX="/usr" 
  make
  make DESTDIR=$PKG install
}
